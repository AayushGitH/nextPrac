"use client"

export default function ErrorPage({error, reset}) {
    return (
        <div>
            <h1>Error is coming from about route</h1>
            <h3>Some unexpected error has caught</h3>
        </div>
    )
}